package vn.id.tozydev.dokja.backend

import kotlin.test.Test
import kotlin.test.assertEquals

class DokjaBackendApplicationTests {
    @Test
    fun `context loads`() {
        assertEquals(2, 1 + 1)
    }
}
