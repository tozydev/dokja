package vn.id.tozydev.dokja.server

import kotlin.test.Test
import kotlin.test.assertEquals

class DokjaServerApplicationTests {
    @Test
    fun `context loads`() {
        assertEquals(2, 1 + 1)
    }
}
